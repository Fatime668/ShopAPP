import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const authSchema = () => {
  return (
    <View>
      <Text>authSchema</Text>
    </View>
  )
}

export default authSchema

const styles = StyleSheet.create({})